return {
  "saghen/blink.cmp",
  opts = {
    keymap = {
      preset = "super-tab",

      -- Optional: If you want <CR> to completely stop accepting completions:
      -- ["<CR>"] = { "accept", "fallback" }, -- default
      -- To make Enter just act like a regular Enter key, uncomment the line below:
      -- ["<CR>"] = { "fallback" },
    },
  },
}
